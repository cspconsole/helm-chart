Thank you for installing {{ .Chart.Name }}!

Your release is named {{ .Release.Name }} and installed in namespace {{ .Release.Namespace }}.

{{- if .Values.global.ingress.enabled }}

Application URLs:
{{- range .Values.global.ingress.hosts }}
  https://{{ . }}
{{- end }}

Services:
  - Report Collector: {{ template "cspconsole.fullname" . }}-report-collector
  - Config Provider: {{ template "cspconsole.fullname" . }}-config-provider

{{- else }}

To access your application, run:
  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ template "cspconsole.fullname" . }}-report-collector 8000:80
{{- end }}
